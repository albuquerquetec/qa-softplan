name: Testes Cypress

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Instalar dependências
        run: npm install

      - name: Rodar testes Cypress
        run: npx cypress run --reporter mochawesome --reporter-options overwrite=false,html=true,json=false --reporter-options reportDir=cypress/reports/mochawesome

      - name: Gerar relatório Mochawesome
        run: |
          npx mochawesome-merge cypress/reports/mochawesome/*.json > cypress/reports/mochawesome/mochawesome.json
          npx marge cypress/reports/mochawesome/mochawesome.json --reportDir cypress/reports/html

      - name: Upload relatório como artefato
        uses: actions/upload-artifact@v4
        with:
          name: relatorio-cypress
          path: cypress/reports/html

      - name: Deploy GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: cypress/reports/html
